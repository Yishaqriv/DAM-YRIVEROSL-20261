import { StyleSheet } from "react-native";

export const ProductListStyles = StyleSheet.create({
    empty: {
        alignItems: "center",
        paddingVertical: 40,
    },
    emptyText: {
        color: "#aaa",
        fontSize: 14,
    },
});
